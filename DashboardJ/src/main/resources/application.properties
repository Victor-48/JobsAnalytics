spring.application.name=DashboardJ

server.port=8080

# Memgraph (Bolt protocol)
graph.uri=bolt://localhost:7687
graph.username=memgraph
graph.password=

# JWT Configuration
jwt.secret=${JWT_SECRET}

# LLM Configuration
llm.provider=ollama
ollama.api.url=http://localhost:11434/api/generate
ollama.model=llama3
openai.api.key=